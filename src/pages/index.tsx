import Head from "next/head"

import { useUserContext } from '~/context/UserContext'
import ClubSearch from '~/components/ClubSearch'

export default function Home() {
  const { handicap, areaOfImprovement, setHandicap, setAreaOfImprovement } = useUserContext()

  return (
    <>
      <Head>
        <title>Golf Club Comparison Tool</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center bg-white text-[#1d1d1f]">
        <div className="container flex flex-col items-center justify-center gap-12 px-4 py-16">
          <h1 className="text-5xl font-extrabold tracking-tight sm:text-[5rem]">
            Compare <span className="text-[hsl(228,100%,70%)]">Golf</span> Clubs
          </h1>
        </div>

        <section className="container mx-auto flex items-center flex-col border border-blue-500 max-w-4xl">
          <div className='border border-[hsla(0, 0%, 59.2%, 0.3)] w-[65%] p-4 rounded-lg bg-gray-200'>
            <h2 className="text-2xl font-bold mb-4">Enter Your Details <span className='font-thin'>(optional)</span></h2>
            <label className="flex flex-col mb-4 border border-[hsla(0, 0%, 59.2%, 0.3)] text-gray-600">
            <span className="mb-1">Approximate handicap:</span>
            <input
              type="text"
              value={handicap}
              onChange={(e) => setHandicap(e.target.value)}
              className="p-2 border rounded-lg w-[90%]"
            />
          </label>

          <label className="flex flex-col mt-8 mb-5 border border-[hsla(0, 0%, 59.2%, 0.3)] text-gray-600">
            <span className="mb-1">What area of your game do you want to improve?</span>
            <input
              type="text"
              value={areaOfImprovement}
              onChange={(e) => setAreaOfImprovement(e.target.value)}
              className="p-2 border rounded-lg w-[90%]"
            />
          </label>
        </div>

        <h1 className="text-2xl font-bold mt-8 mb-4">Search for Golf Clubs</h1>
          <ClubSearch />
        </section>
      </main>
    </>
  )
}
